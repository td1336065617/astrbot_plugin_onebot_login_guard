# 更新日志

本文件记录所有值得注意的变更，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

图例：✨ 新增 · 🐛 修复 · ⚙️ 变更

---

## [0.1.11] - 2026-09-26

> 🐛 修复：状态页整页不可用 —— 一个裸换行让整段脚本语法错误，页面只剩标题；桥接也没接上、字段还多解包了一层。

### 🐛 修复
- BUG-001：删掉「自动识别」里那三处裸换行（改为数组 join 拼多行），页面脚本恢复可解析。
- BUG-002：页面显式引入 bridge-sdk（服务端注入点在 </body> 之前，晚于内联脚本执行）。
- BUG-003：去掉二次解包（res.data.x → res.x），共 5 处。
- BUG-004：api.ready() 改为 Promise 形式；检测不到桥接时给出明确提示而不是静默 refresh。
- BUG-005：3 处 alert 改为页内提示条（插件页沙箱没有 allow-modals，alert 是空操作）。
- BUG-006：二维码按实例取（qr_data 支持可选 instance_id，后端保持向后兼容），失败时显示「暂无可用二维码」。
- BUG-007：所有插值统一过 esc()，避免昵称/群名里的标签注入。

### ✅ 验证
- tools/check_inline_js.py 对该页返回 0（修复前为 1，正是 BUG-001）；guard 全量 89 条用例通过。

---

## [0.1.10] - 2026-09-26

> 🐛 修复：菜单里漏了两条指令，而且是表格格式（菜单渲染器提取不出来，还会多出一条「|」）。

### 🐛 修复
- menu.md 改为项目符号列表，并补齐 5 条指令：状态 / 二维码 / 刷新二维码 / 识别 / 测试（BUG-031）。

---
## [0.1.9] - 2026-09-26

> 🛡️ 修复：账号被腾讯踢下线后，守护仍报「在线」，既不提醒也不推码（生产静默失联 3 小时）。

### 🐛 修复
- **新增权威判据：协议端自报登录态**（core/probes.py 的 ProtocolStatusProbe）——
  直接读 NapCat WebUI 的 CheckLoginStatus（isLogin / isOffline / loginPhase / qrcodeurl），
  这是唯一能一眼看穿「僵尸反向 WS」的证据；复用实例里已有的地址与 token，不新增配置项。
- **收紧 AstrBot 侧判据**：反向 WS 客户端集合非空不再等于在线，
  必须再做一次只读 API 往返（get_login_info，5 秒超时）；往返失败即判「掉线（疑似僵尸连接）」。
- **过期二维码语义修正**：文件存在但已过期不再「不表态」，而是判为需要登录并带 stale=True，
  从而触发「主动向协议端申请全新二维码」；死码依旧不会作为图片发出（既有行为不变）。
- 未配置 WebUI 地址/token 时如实标注「无法判定」，不影响其它探测源。

### ✅ 验证
- 新增 11 条用例（协议端四种自报态 / 僵尸 WS / 过期码语义 / 源优先级 / 死码不发图），全量 89 条通过。
- 生产复现依据：CheckLoginStatus 返回 {"isLogin": false, "isOffline": true, "loginPhase": "waiting_qrcode"}，
  而 AstrBot 侧 _wsr_* 集合仍非空 → 旧判据误报在线。

---

## [0.1.8] - 2026-09-23

> 🐛 修复：扫码被驳回后，「登录守护二维码」还在发那张已失效的码。

### 🐛 修复
- 根因：扫码被驳回后旧二维码其实已经失效，但协议端写下的文件 mtime 还很新，
  光看「新鲜度」分辨不出来，于是插件把这张死码又推了一遍。
- 「登录守护二维码」（以及状态页按钮、POST /refresh_qr）现在**每次都向协议端申请一张全新二维码**再推送，
  不再重发缓存里那张。
- 没能让协议端重新出码时如实告知（提示检查 WebUI 地址与 token），不假装成功。
- 状态页移除「重发二维码」按钮（那正是发缓存码的路径）。

---

## [0.1.7] - 2026-09-23

> 🐛 通知发送失败会重试（修复「插件启动时平台还没加载完，首次告警直接丢失」）。

### 🐛 修复
- 实测：插件在 16:50:56 启动并探测到「需要登录」，但平台适配器 16:51:43 才加载完成，
  首次推送必然失败；而 need_login 只在状态跳变时发一次，这一丢就永远收不到。
- 现在所有渠道都失败时会把事件挂起，按 _retry_interval（默认 60 秒）重试，
  最多 _retry_limit（默认 10）次；任一渠道成功即清除挂起。

- 二维码刷新推送按实例节流（qr_refresh_notify_interval 默认 600 秒）：
  协议端等待扫码时会每两分钟出一张新码，不节流会变成刷屏。

---

## [0.1.6] - 2026-09-23

> ✨ **主动刷新二维码**：协议端不再自动出码时，插件会自己让协议端重新生成一张并推送。

### ✨ 新增
- 新增 core/napcat_api.py：NapCat WebUI 客户端（POST /api/auth/login 用 SHA256(token + ".napcat") 换 JWT → POST /api/QQLogin/RefreshQRcode 重新出码）。
- 二维码过期或缺失时，插件**自动请求协议端重新生成**（auto_refresh_qr 默认开，qr_refresh_cooldown 默认 120 秒），成功后立刻把新码推给你。
- 新增指令「登录守护刷新二维码」、接口 POST /refresh_qr、状态页「刷新二维码」按钮，可手动强制刷新。
- WebUI 地址与 token 可由「登录守护识别」自动从协议端 webui.json 读取，通常无需手填。

### ⚙️ 变更
- 「二维码已过期」的提示改为引导直接刷新，不再让人去重启协议端。

---

## [0.1.5] - 2026-09-23

> 🐛 **设计修正（第二轮）**：用「协议端进程是否存活」区分「掉登录」与「进程挂了」，
> 并在二维码过期时给出可执行指引，而不是静默。

### 🐛 修复
- **修正上一版的过度否决**：实测 NapCat 掉登录后生成一次二维码，若没人扫，它**不会一直刷新**
  （12:29 生成 → 12:31 重试一次 → 之后 4 小时无动作）。因此「二维码文件过期」**不等于**「不需要登录」。
- 新增 ProcessProbe：反向 WS 没连上时，
  - 协议端进程还在 → 判为 **需要登录**（NapCat 登录后才连 WS）；
  - 进程不在 → 判为 **掉线**。
- 新增 qr_expired 事件：二维码由「新鲜」变「过期」时提醒一次，文案给出可执行动作
  （去协议端 WebUI 重新发起登录 / 重启协议端服务），不再静默。
- need_login 事件在二维码已过期时不再附带图片（避免推死码），改为文字指引。
- **状态显示与判定一致**：掉线状态立即反映到状态页，只有通知仍做抖动确认
  （此前会出现「状态：未知（反向 WebSocket 无客户端连接）」这种自相矛盾的显示）。
- 指令「登录守护二维码」在无码可推时给出针对性提示（等扫码但码过期 / 进程没运行 / 已恢复）。

---

## [0.1.4] - 2026-09-23

> 🐛 **设计修正**：过期证据不得用于判定当前状态（修复「一直推送过期二维码」）。

### 🐛 修复
- **日志新鲜度**：协议端运行时会持续写日志；超过 log_fresh_seconds（默认 900 秒）没更新的日志视为无效，其内容不再参与判定。此前「上一次崩溃留下的死日志」里那句「请扫描下面的二维码」会被永远当成当前状态。
- **二维码过期即否决**：二维码文件存在但已过期时标记 stale，既不能作为「需要登录」的依据，也不会作为图片发出去。
- **UNKNOWN 不再无限保留旧状态**：连续 unknown_confirm_rounds（默认 3）轮拿不到有效证据时，状态降级为「未知」并清空二维码，停止推送。
- **重发指令**拒绝推送已过期的二维码，改为提示「等协议端刷新后会自动推送」。
- **自动识别不再采用死日志**：只接受 1 小时内更新过的日志文件。
- WebUI 状态页显示「⚠️ 二维码已过期」。

### ⚙️ 变更
- 判定原则改为**新鲜证据优先**：新鲜二维码 > 新鲜日志 > WebUI > 平台连接状态；任何过期证据一律忽略。
- 新增配置项 log_fresh_seconds、unknown_confirm_rounds。

---

## [0.1.3] - 2026-09-23

> 🐛 修复「登录守护二维码」推送过期二维码。

### 🐛 修复
- resend_qr 之前直接发探测时留下的**二维码快照**，协议端刷新二维码后推的就是旧码。现在会**先重新探测一次**，拿协议端当前的二维码再发。
- 只有实例确实处于「需要登录」时才推送；已恢复登录时提示「当前不需要扫码」，不再推旧码。
- WebUI 二维码预览（/qr、/qr_data）同样改为优先读取协议端的实时文件，其次才是快照副本。

### ✨ 新增
- InstanceStatus 增加 qr_source_path（协议端原始路径），与发送用的快照副本区分开。

---

## [0.1.2] - 2026-09-23

> ✨ 协议端路径支持自动识别；没有日志文件也能判断「需要登录」。

### ✨ 新增
- 新增 core/autodetect.py：扫描本机协议端进程（/proc 的 exe 与 cwd）与常见安装目录，自动识别 qr_path / log_path，并从 NapCat 的 webui.json 读取 http_url / http_token。
- 新增指令「登录守护识别」、接口 GET /discover 与 POST /apply_discovery；状态页新增「自动识别协议端」按钮。
- 新增配置项 auto_detect（默认开）与 qr_fresh_seconds（默认 300）。
- QrFileProbe 支持「文件新鲜度」判定：实测协议端登录成功后**不会删除**二维码文件，因此按 mtime 判断是否正在等待扫码 —— 只配 qr_path 也能发现掉登录。

### ⚙️ 变更
- 探测优先级：新鲜的二维码文件 > 协议端日志 > WebUI > 平台连接状态。
- 启动时若日志/二维码/WebUI 路径为空，自动识别并回填（只填空字段，不覆盖手填内容）。

---

## [0.1.1] - 2026-09-23

> ✨ 配置页的「实例」与「通知会话」改为下拉选择，不再手输。

### ✨ 新增
- instances 改用 template_list：每个实例渲染为子表单，platform_id 变成**下拉框**（选项 = AstrBot 已加载的平台实例，运行时注入）。
- notify_sessions 变成**可搜索多选下拉**：选项 = 机器人见过的会话 + AstrBot 会话库里的记录。
- 新增「会话观测」：任何平台的消息到达时记录其 unified_msg_origin，作为通知会话候选（让目标会话先给机器人发一条消息即可出现在下拉里）。
- 新增 core/schema_options.py：平台实例枚举、会话枚举、schema 选项注入。

### ⚙️ 变更
- 旧配置里的实例条目会在启动时自动补上 template_list 需要的 __template_key（幂等，只写一次）。

---

## [0.1.0] - 2026-09-23

首个版本。

### ✨ 新增
- 四路探测：协议端日志、二维码文件、协议端 WebUI（可选）、AstrBot 平台连接状态。
- 状态机：ONLINE / OFFLINE / NEED_LOGIN / UNKNOWN，含掉线确认与去重冷却。
- 通知渠道：AstrBot 会话、企业微信、钉钉、飞书、Telegram、通用 Webhook、SMTP 邮件。
- 多协议端多账号：instances 列表，各自独立探测源、状态与冷却。
- WebUI 状态页 + REST 接口（status / probe / resend_qr / test_notify / events / qr / qr_data）。
- 指令：登录守护状态 / 登录守护二维码 / 登录守护测试。
- 事件落盘（events.jsonl）与二维码副本保存。
